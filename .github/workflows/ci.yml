name: C CI Pipeline

on:
  push:
    branches: [ "main" ]   # যদি তোমার default branch master হয়, এখানে master লিখো
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4   # সর্বশেষ ভার্সন

    - name: Install dependencies
      run: sudo apt-get update && sudo apt-get install -y build-essential

    - name: Build project
      run: make

    - name: Run basic test
      run: |
        echo "Running HMS program..."
        ./hms <<EOF
        0
        EOF

    - name: Upload binary artifact
      uses: actions/upload-artifact@v4   # v3 এর জায়গায় v4
      with:
        name: hms-binary
        path: hms